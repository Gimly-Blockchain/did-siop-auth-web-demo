{
  "name": "onto-web-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "start-server": "cd onto-demo-server && yarn run start",
    "start-client": "cd onto-demo-client && yarn run start",
    "build": "cd onto-demo-client && yarn run build",
    "start": "concurrently \"yarn start-server\" \"yarn start-client\""
  },
  "dependencies": {
    "dotenv-flow": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^1.0.0"
  }
}
